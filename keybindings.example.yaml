# LazyPost Keybindings Configuration
# Copy this to ~/.config/lazypost/keybindings.yaml or ./keybindings.yaml

keybindings:
  quit: ctrl-c
  quit-alt: q
  next-view: tab
  prev-view: '['
  send-request: ctrl-s
  start-editor: ':'
  switch-method: ctrl-m
